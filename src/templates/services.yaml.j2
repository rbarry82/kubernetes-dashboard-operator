---
kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: {{ app_name }}
  name: {{app_name}}-metrics-scraper
  namespace: {{ namespace }}
spec:
  ports:
    - port: 8000
      targetPort: 8000
  selector:
    k8s-app: {{ app_name }}